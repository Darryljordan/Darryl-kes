import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OnboardingPage4Page } from './onboarding-page4.page';

describe('OnboardingPage4Page', () => {
  let component: OnboardingPage4Page;
  let fixture: ComponentFixture<OnboardingPage4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OnboardingPage4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
